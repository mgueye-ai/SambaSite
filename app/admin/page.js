import AdminView from '../../components/AdminView';

export const metadata = {
  title: 'Samba Team Admin',
  description: 'Samba platform administration — organizers, events, and impersonation.',
};

export default function AdminPage() {
  return <AdminView />;
}
